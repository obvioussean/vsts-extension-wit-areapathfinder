// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "scripts/*.js": true,
        "**/*.js": {
            "when": "$(basename).ts"
        },
        "**/*.vsix": true,
        "**/*.map": true,
        "*.gitignore": true
    }
}